[variables]
PYTHON_VERSION = "3.12"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --worker-class gthread --threads 2 --timeout 120"
